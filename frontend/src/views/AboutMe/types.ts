export type TechInfoProps = {
  name: string;
  description: string;
  link: string;
};

export type FormData = {
  name: string;
  email: string;
  cellphone: string;
  subject: string;
  message: string;
};
